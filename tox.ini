[testenv]
deps =
    pytest
    coverage[toml]
    pytest-mock
passenv = *
commands = coverage run -m pytest tests {posargs}

[testenv:report]
deps =
    coverage[toml]
skip_install = true
commands =
    coverage report -m
